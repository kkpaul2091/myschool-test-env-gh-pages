apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "myschool.fullname" . }}-test-connection"
  labels:
    {{- include "myschool.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "myschool.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
