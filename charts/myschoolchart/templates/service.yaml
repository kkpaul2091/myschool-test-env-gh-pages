apiVersion: v1
kind: Service
metadata:
  name: {{ include "myschool.fullname" . }}
  labels:
    {{- include "myschool.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
      nodePort: {{ .Values.service.nodePort }}
  selector:
    {{- include "myschool.selectorLabels" . | nindent 4 }}
