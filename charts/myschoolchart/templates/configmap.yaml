apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "myschool.fullname" . }}-config
  labels:
    {{- include "myschool.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.configMapData }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
